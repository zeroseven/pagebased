{
	"name": "zeroseven/pagebased-registration-fixture",
	"description": "Test fixture: registers a full pagebased registration (incl. list plugin) at boot",
	"type": "typo3-cms-extension",
	"require": {
		"typo3/cms-core": "^13.4 || ^14.0"
	},
	"extra": {
		"typo3/cms": {
			"extension-key": "RegistrationFixture"
		}
	}
}
