registration {
    object {
        # cat=object/enable/10; type=options[default=,enable=1,disable=0]; label=Enable/disable Tags for {{ cookiecutter.__object_class_name|lower }} pages
        tags =
        # cat=object/enable/20; type=options[default=,enable=1,disable=0]; label=Enable/disable Top for {{ cookiecutter.__object_class_name|lower }} pages
        top =
        # cat=object/enable/30; type=options[default=,enable=1,disable=0]; label=Enable/disable relations between {{ cookiecutter.object_name|lower }} pages
        relations =
        # cat=object/enable/40; type=string; label=Topics;Comma separated list of topic storage page ids
        topicPageIds =
        # cat=object/enable/50; type=string; label=Contacts;Comma separated list of contact storage page ids
        contactPageIds =
        # cat=object/enable/60; type=string; label={{ cookiecutter.__object_class_name }}-Overlay-Icon identifier
        overlayIconIdentifier =
    }
    category {
        # cat=category/enable/20; type=string; label=Category icon identifier
        iconIdentifier =
    }
    listPlugin {
        # cat=listPlugin/enable/20; type=string; label=List icon identifier
        iconIdentifier =
    }
    filterPlugin {
        # cat=filterPlugin/enable/20; type=string; label=Filter icon identifier
        iconIdentifier =
    }
}
