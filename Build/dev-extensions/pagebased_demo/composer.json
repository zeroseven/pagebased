{
	"name": "zeroseven/pagebased-demo",
	"description": "Dev-only demo consumer for pagebased: registers a \"News\" object/category type so the seed data is classified by pagebased. Not published, loaded only in the .build dev instance via a path repository.",
	"license": "GPL-2.0-only",
	"type": "typo3-cms-extension",
	"version": "1.0.0",
	"require": {
		"typo3/cms-core": "^13.4 || ^14.0"
	},
	"autoload": {
		"psr-4": {
			"Zeroseven\\PagebasedDemo\\": "Classes/"
		}
	},
	"extra": {
		"typo3/cms": {
			"extension-key": "pagebased_demo"
		}
	}
}
